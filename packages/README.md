# SSVproff Packages

Эта директория содержит все рабочие пакеты (workspaces) монорепозитория SSVproff.

## 📦 Структура

```
packages/
├── api/          # Backend API (FastAPI/Express)
└── web/          # Frontend Web (Next.js/React)
```

## 🚀 Быстрый старт

### Установка зависимостей

```bash
# В корне проекта
pnpm install
```

### Разработка

```bash
# Запустить все пакеты параллельно
pnpm dev

# Запустить только API
pnpm dev:api

# Запустить только Web
pnpm dev:web
```

### Сборка

```bash
# Собрать все пакеты
pnpm build

# Собрать только API
pnpm build:api

# Собрать только Web
pnpm build:web
```

### Тестирование

```bash
# Запустить тесты во всех пакетах
pnpm test

# Тесты только для API
pnpm test:api

# Тесты только для Web
pnpm test:web
```

### Линтинг

```bash
# Запустить линтер для всех пакетов
pnpm lint

# Исправить проблемы автоматически
pnpm lint:fix
```

## 📋 Создание нового пакета

1. Создайте директорию в `packages/`:

```bash
mkdir packages/new-package
cd packages/new-package
```

2. Создайте `package.json`:

```json
{
  "name": "@ssvproff/new-package",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "...",
    "build": "...",
    "test": "..."
  }
}
```

3. Установите зависимости в корне:

```bash
pnpm install
```

## 🔗 Зависимости между пакетами

Для использования одного пакета в другом:

```json
{
  "dependencies": {
    "@ssvproff/api": "workspace:*"
  }
}
```

## 📚 Дополнительная информация

- [pnpm Workspaces](https://pnpm.io/workspaces)
- [Turborepo Docs](https://turbo.build/repo/docs)
- [Основной README](../README.md)
